# Contributing / 参与贡献

感谢你愿意帮助扩展路由器兼容性。

## 最重要的原则

**不要把“理论可运行”写成“已支持”。**

设备只有完成对应层级的真实验证后，才能在兼容矩阵中升级状态。

## 新设备建议流程

1. 先确认厂商官方是否已经提供网易 UU远程 / WOL 支持；如果有，优先使用官方方案。
2. 如果需要社区适配，先取得合法、明确授权的 SSH/root Shell 权限。
3. 运行仓库提供的只读 `scripts/collect-info.sh`。
4. 检查报告，确认其中没有个人敏感信息。
5. 通过 GitHub Issue 提交“New Router Support / 新路由器支持”请求。
6. 未经真实硬件验证，不要直接把新设备加入 `Verified`。

## 验证等级

- Detect：平台与架构可识别。
- Install：插件安装成功。
- Reboot：真实重启后能自动恢复。
- Cloud：UU 云控制连接健康。
- App：UU主机加速 App 能识别路由器。
- Binding：UU远程能检测到辅助设备并完成 PC 配置。
- Remote WOL：手机关闭 Wi‑Fi，仅使用移动数据完成真实开机。

只有最后一项真实通过，才标记 `Remote WOL = Verified`。

## 安全边界

提交 Issue 或 PR 前，请自行检查并移除不应公开的账号、设备身份、网络身份和认证信息。仓库不接收网易闭源二进制的重新分发。

## AI 生成代码

欢迎使用 AI 辅助开发，但请在 PR 中说明哪些部分由 AI 参与生成，并提供实际测试结果。AI 推测不能替代硬件验证。
