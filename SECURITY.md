# Security Policy / 安全说明

## 项目边界

本项目不会：

- 提供漏洞利用来获取路由器 root；
- 绕过厂商安全限制开启 SSH；
- 收集或要求用户提交路由器密码、PPPoE 密码、UU账号密码、验证码、Token；
- 在未知设备上默认执行写入操作；
- 在未确认可回滚前修改生产防火墙或启动链。

## 手动适配的权限要求

社区适配模式通常需要 SSH/root Shell 权限。用户应通过设备厂商、固件项目或其他合法、可信的方式自行获得管理权限。

未知设备默认执行只读环境检测，不直接安装。

## 敏感信息

Issue、日志、截图和诊断报告中应隐藏：

- WAN/Public IP
- LAN MAC（除非确有必要且用户明确同意）
- Router SN
- PPPoE 账号与密码
- UU账号/手机号
- UU设备验证码
- Token / Cookie / Session
- SSH private key
- 路由器 root 密码

计划中的 `collect-info.sh` 必须以“默认脱敏”为设计要求，而不是依赖用户手工删除隐私。

## 第三方二进制

本仓库不计划重新分发网易 UU 闭源二进制。安装流程应从网易官方来源动态获取，并验证官方提供的完整性信息。

## 风险提示

修改路由器启动、防火墙和策略路由存在断网或失去远程管理的风险。正式 adapter 必须提供：

1. 平台前置检测；
2. 安装前备份；
3. 最小修改；
4. 状态检查；
5. 明确回滚；
6. 真实 reboot 验证。

## 漏洞与安全问题报告

仓库公开后，如果问题涉及凭据泄露、命令注入、危险默认行为或其他安全问题，不应直接在公开 Issue 中贴敏感内容。公开前将补充私密安全报告入口。
